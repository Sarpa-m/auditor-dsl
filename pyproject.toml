[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "auditor-dsl"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["ply==3.11", "colorama"]

[tool.setuptools.packages.find]
where = ["src"]
